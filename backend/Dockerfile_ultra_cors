FROM python:3.9-slim

WORKDIR /app

# Copy requirements first for better caching
COPY requirements_minimal.txt .
RUN pip install --no-cache-dir -r requirements_minimal.txt

# Copy the ultra CORS app
COPY app_ultra_cors.py app.py

EXPOSE 8000

CMD ["python", "app.py"]
